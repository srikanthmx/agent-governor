{
  "name": "agent-governor",
  "version": "0.1.0",
  "private": true,
  "description": "Local-first AI agent governance control plane.",
  "type": "module",
  "scripts": {
    "agent": "pnpm --filter @agent-governor/cli agent",
    "build": "pnpm -r build",
    "dev": "pnpm --parallel dev",
    "doctor": "pnpm agent doctor",
    "lint": "pnpm -r lint",
    "setup": "pnpm agent setup",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "@types/node": "^20.14.15",
    "tsx": "^4.16.2",
    "typescript": "^5.5.4"
  },
  "packageManager": "pnpm@9.6.0"
}
